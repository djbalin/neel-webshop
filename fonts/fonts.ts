import { Anton, Open_Sans } from "next/font/google";

export const openSans = Open_Sans({
  subsets: ["latin"],
  weight: "variable",
});

export const anton = Anton({
  subsets: ["latin"],
  weight: ["400"],
});
